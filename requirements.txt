# Abhängigkeiten für das DASHBOARD (Streamlit Cloud + lokale Anzeige).
# Bewusst schlank – das Dashboard liest nur data/output/latest.json & portfolio.json.
# Die Analyse-Pipeline (GitHub Actions) nutzt die volle Liste in requirements-ci.txt.
streamlit>=1.30
pandas>=2.0
